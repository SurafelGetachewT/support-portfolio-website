# Certificates

Place certificate PDFs or images in this folder, then update the `certificates`
array in `src/data/portfolioData.js`.

Example:

```js
{
  title: "Customer Support Certificate",
  issuer: "Course or platform name",
  status: "Available",
  href: "certificates/customer-support-certificate.pdf",
}
```
